import { ConnectButton } from './WebConnectButton';

export const Header = () => {
    return (
        <header>
            <h1>OPUS POOL</h1>
            <ConnectButton />
        </header>
    );
};
